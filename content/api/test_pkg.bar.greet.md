+++
title = "test_pkg.bar.greet"
+++

greet(name: str) -> str
[source](test_pkg/bar.py#L15-L30)

Return a greeting message. This function is part of [The `bar` Module](@/api/test_pkg.bar.md)


Args:
    name (str): Name to greet.

Returns:
    str: Greeting message.

Example:
    >>> greet("Anna")
    'Hello, Anna!'
