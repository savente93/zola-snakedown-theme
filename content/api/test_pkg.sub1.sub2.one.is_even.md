+++
title = "test_pkg.sub1.sub2.one.is_even"
+++

is_even(num: int) -> bool
[source](test_pkg/sub1/sub2/one.py#L11-L28)

Check if a number is even.

Args:
    num (int): Number to check.

Returns:
    bool: True if even, else False.

Example:
    >>> is_even(4)
    True

See Also:
    [test_pkg.sub1.sub2.one.is_odd](@/api/test_pkg.sub1.sub2.one.is_odd.md)
